<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>편집 창</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h2 class="mb-0">편집하기</h2>
                </div>
                <div class="card-body">
                    <form action="/todos/update" method="post">
                        <input type="hidden" name="id" value="{{todos.id}}">
                        <div class="mb-3">
                            <label for="title" class="form-label">제목</label>
                            <input type="text" class="form-control" id="title" name="title" value="{{todos.title}}" required>
                        </div>

                        <div class="mb-3">
                            <label for="description" class="form-label">설명</label>
                            <input type="text" class="form-control" id="description" name="description" placeholder="한 줄로 간략하게 작성하세요" value="{{todos.description}}">
                        </div>

                        <div class="mb-3">
                            <label for="createdAt" class="form-label">생성일</label>
                            <input type="datetime-local" class="form-control" id="createdAt" name="createdAt" value="{{todos.createdAt}}">
                        </div>

                        <div class="mb-3">
                            <label for="dueDate" class="form-label">마감일</label>
                            <input type="datetime-local" class="form-control" id="dueDate" name="dueDate" value="{{todos.dueDate}}">
                        </div>

                        <div class="mb-3">
                            <div class="form-check">
                                <input class="form-check-input" type="radio" name="completed" id="radioDefault1" value="true" {{#todos.completed}}checked{{/todos.completed}}>
                                <label class="form-check-label" for="radioDefault1">
                                    <span class="badge bg-success">완료됨</span>
                                </label>
                            </div>
                            <div class="form-check">
                                <input class="form-check-input" type="radio" name="completed" id="radioDefault2" value="false" {{^todos.completed}}checked{{/todos.completed}}>
                                <label class="form-check-label" for="radioDefault2">
                                    <span class="badge bg-warning">진행 중</span>
                                </label>
                            </div>
                        </div>

                        <div class="d-flex justify-content-between mt-4">
                            <a href="/todos/{{todos.id}}" class="btn btn-secondary">뒤로가기</a>
                            <div class="d-flex align-items-center gap-2">
                                <a href="/todos/delete/{{todos.id}}"
                                   class="btn btn-danger"
                                   onclick="return confirm('정말 삭제하시겠습니까?')">삭제</a>
                                <button type="submit" class="btn btn-primary">편집</button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Bootstrap JS Bundle with Popper -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

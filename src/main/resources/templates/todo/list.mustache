<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>할 일 목록</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-10">
            <div class="card shadow">
                <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
                    <h2 class="mb-0">할 일 목록</h2>
                    <a href="/todos/new" class="btn btn-light">
                        <i class="fas fa-plus me-1"></i>새 할 일
                    </a>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead>
                            <tr>
                                <th>제목</th>
                                <th>설명</th>
                                <th>마감일</th>
                                <th>생성일</th>
                                <th>상태</th>
                            </tr>
                            </thead>
                            <tbody>
                            {{#todos}}
                                <tr class="todo-row" onclick="location.href='/todos/{{id}}';" style="cursor: pointer;">
                                    <td>{{title}}</td>
                                    <td>{{description}}</td>
                                    <td>{{formattedDueDate}}</td>
                                    <td>{{formattedCreatedAt}}</td>
                                    <td>
                                        <span class="badge {{#completed}}bg-success{{/completed}}{{^completed}}bg-warning{{/completed}}">
                                            {{#completed}}완료됨{{/completed}}
                                            {{^completed}}진행 중{{/completed}}
                                        </span>
                                    </td>
                                </tr>
                            {{/todos}}
                            </tbody>
                        </table>
                    </div>

                    {{^todos}}
                        <div class="text-center py-5">
                            <i class="fas fa-clipboard-list fa-4x mb-3 text-muted"></i>
                            <p class="lead">등록된 할 일이 없습니다. 새로운 할 일을 추가해보세요!</p>
                            <a href="/todos/new" class="btn btn-primary mt-3">할 일 추가하기</a>
                        </div>
                    {{/todos}}
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Bootstrap JS Bundle with Popper -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>